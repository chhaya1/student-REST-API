name: Update Image Tag

on:
  push:
    branches:
      - main 

jobs:
  update-image-tag:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Git
        run: |
          git config --global core.attributesfile ~/.gitattributes
          git config user.name "chhaya1"
          git config user.email "chhayasharma1401@gmail.com"

      - name: Update Image Tag
        run: |
          IMAGE_TAG=$(date +%Y%m%d%H%M%S)
          echo "New Image Tag: $IMAGE_TAG"

          # Update only your values.yaml file
          sed -i "s|image: .*|image: chhaya786/student-api:$IMAGE_TAG|" helm/student-api/values.yaml

          cd $GITHUB_WORKSPACE
          git config --global --add safe.directory $GITHUB_WORKSPACE

          # Disable Git attributes scanning
          git config --global core.attributesfile /dev/null

          git add helm/student-api/values.yaml
          git commit -m "Update image tag to $IMAGE_TAG"
          git push origin main
